# Publish + login to the official npm registry for this package.
# The global config points at npmmirror (cnpm); this overrides it for everything
# run inside this directory (login, install, publish).
registry=https://registry.npmjs.org/